# Usar versões exatas das dependências
save-exact=true

# Instalar peer dependencies automaticamente
auto-install-peers=true

# Usar o registry público do npm
registry=https://registry.npmjs.org/

# Verificação de integridade do lockfile
strict-peer-dependencies=true